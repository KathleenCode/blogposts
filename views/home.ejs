<html lang="en">

  <%- include('./partials/header.ejs') %>

  <body>
    <%- include('./partials/nav.ejs') %>

    <div class="blogs content">
    <h1>All Blogs</h1>
    
     <% if(blogs.length > 0) { %>
     <% blogs.forEach(blog => { %>

        <!-- To make it clickable -->
      <a class="single" href="/blogs/<%= blog._id %>" >
      <h3 class="title"><%= blog.title %></h3>
      <p class="snippet"><%= blog.description %></p>
      </a>

      <!--p><!%= blog.description %></p to display the body -->

    <% }) %>
    <% } else { %>
      <p>There are no blogs shown. Add one here <a href="/blogs/create"></a></p>
    <% } %>
  </div>

  <%- include('./partials/footer.ejs') %>
  </body>
</html>




<!-- you can write the for each using for loop too-->
  <!--% for(let i = 0; i < blogs.length; i++){ %>
  <!--% if(blogs){ %>
    <h3><!--%= blogs[i].title %></h3>
    <p><!--%= blogs[i].description %></p>
  <!--% }else{ %>
    <p>There are no blogs to show. You can add one by going <a href="/blogs/create">here</a></p>
  <!--% } %>
  <!--% } %>